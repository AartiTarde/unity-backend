# ===============================================
# Voter ID Extraction System - Configuration
# ===============================================
# Copy this file to .env and fill in your values

# --- Server Configuration ---
DEBUG=False
# Set to True only for development. NEVER use True in production!

# File retention period (hours)
FILE_RETENTION_HOURS=24

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# --- Authentication (Optional) ---
AUTH_ENABLED=False
# Set to True to enable API key authentication
# API_KEYS=your_api_key_1,your_api_key_2
# Generate keys using: python auth.py

# --- Google Cloud Vision API Configuration (Optional but Recommended) ---
# Used for OCR when local Tesseract fails or returns empty results
# Also used for region detection and intelligent text formatting
# Reduces API calls by only using when needed
#
# Method 1 (Recommended): Service Account JSON file
# Download service account JSON from Google Cloud Console and provide path:
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json
#
# Method 2 (Legacy): API Key (deprecated, use GOOGLE_APPLICATION_CREDENTIALS instead)
# GOOGLE_VISION_API_KEY=your_google_vision_api_key_here

# --- OCR Configuration ---
# Tesseract path (auto-detected on Windows, set manually for Linux/Mac)
# TESSERACT_CMD=/usr/bin/tesseract

# OCR DPI setting (300-600, higher = better quality but slower)
OCR_DPI=400

# Confidence threshold for using Google Vision API fallback (0.0-1.0)
# Lower = more Google Vision API calls, Higher = more local processing
OCR_CONFIDENCE_THRESHOLD=0.7

# --- Performance Settings ---
# Maximum concurrent background tasks
MAX_BACKGROUND_WORKERS=3

# Maximum PDF file size (bytes)
# Default: 50MB = 52428800
# MAX_FILE_SIZE=52428800

# --- Logging ---
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# --- Notes ---
# 1. Google Vision API is OPTIONAL - system works without it
# 2. Without Google Vision: Uses local Tesseract OCR (free, slightly less accurate)
# 3. With Google Vision: Better accuracy, but costs ~$0.001-0.01 per page
# 4. Recommended: Start without Google Vision, enable if accuracy is insufficient
# 5. For Hindi text support, install Tesseract Hindi language data

# --- Quick Start ---
# 1. Copy this file: cp env.example.txt .env
# 2. Edit .env with your values
# 3. Restart server: python app.py
# 4. Check logs: tail -f app.log

